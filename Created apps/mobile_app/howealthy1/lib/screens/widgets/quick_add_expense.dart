import 'package:flutter/material.dart';
class QuickAddExpense extends StatelessWidget { const QuickAddExpense({super.key});
  @override Widget build(BuildContext context) => Container(height: 300, color: Colors.black, child: const Center(child: Text('Quick Add Expense')));
}
