import 'package:flutter/material.dart';
class OracleChatPage extends StatelessWidget { const OracleChatPage({super.key});
  @override Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('Oracle AI')), body: const Center(child: Text('Chat')));
}
